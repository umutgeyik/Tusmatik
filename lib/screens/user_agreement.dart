import 'package:flutter/material.dart';

class UserAgreementScreen extends StatelessWidget {
  static const String id = 'user_agreement_screen';
  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}